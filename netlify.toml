[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side routes always resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
